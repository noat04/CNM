<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TMA Store - Chào mừng</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        .hero-bg {
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)),
                        url('https://images.unsplash.com/photo-1441986300917-64674bd600d8?q=80&w=2070&auto=format&fit=crop');
            background-size: cover;
            background-position: center;
            height: 100vh;
            display: flex;
            align-items: center;
            color: white;
        }
    </style>
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top shadow">
        <div class="container">
            <a class="navbar-brand fw-bold" href="/"><i class="bi bi-box-seam me-2"></i>TMA Store</a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarContent">
                <div class="ms-auto d-flex align-items-center">

                    <% if (locals.user) { %>
                        <div class="text-white me-3 d-none d-lg-block">
                            Xin chào, <span class="fw-bold text-warning"><%= user.username %></span>
                            <% if (user.role === 'admin') { %>
                                <span class="badge bg-danger ms-1">ADMIN</span>
                            <% } else { %>
                                <span class="badge bg-secondary ms-1">STAFF</span>
                            <% } %>
                        </div>

                        <a href="/auth/logout" class="btn btn-outline-danger btn-sm">
                            <i class="bi bi-box-arrow-right me-1"></i>Đăng xuất
                        </a>

                    <% } else { %>
                        <a href="/auth/login" class="btn btn-outline-light me-2">Đăng nhập</a>
                        <a href="/auth/register" class="btn btn-primary">Đăng ký</a>
                    <% } %>

                </div>
            </div>
        </div>
    </nav>

    <header class="hero-bg">
        <div class="container text-center">
            <h1 class="display-1 fw-bold">Chào mừng đến với TMA Store</h1>
            <p class="lead fs-3 mb-5">Hệ thống quản lý kho hàng hiện đại trên nền tảng Cloud.</p>

            <div class="d-grid gap-3 d-sm-flex justify-content-sm-center">
                <% if (locals.user) { %>
                    <a href="/products" class="btn btn-success btn-lg px-5 shadow">
                        <i class="bi bi-box-seam me-2"></i>Vào kho hàng
                    </a>
                    <% if (user.role === 'admin') { %>
                        <a href="/categories" class="btn btn-outline-light btn-lg px-5">Quản lý danh mục</a>
                    <% } %>
                <% } else { %>
                    <a href="/auth/login" class="btn btn-light btn-lg px-5">Đăng nhập để bắt đầu</a>
                <% } %>
            </div>
        </div>
    </header>

    <footer class="bg-dark text-white py-4 fixed-bottom">
        <div class="container text-center">
            <p class="mb-0">&copy; 2026 TMA Internship Project. Phát triển bởi Toàn.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>